# LangGraph curriculum — 4 lessons

A code-along curriculum teaching the LangGraph patterns this repo uses. Written on **Day 6**
of the build (see `plans/PRD-1-witus-triage-agent.md` §14).

Planned files:

1. `01-chain-to-graph.md` — when you actually need LangGraph instead of a plain chain.
2. `02-agent-state.md` — designing the state object (`TriageState` as the worked example).
3. `03-tools-and-approval.md` — Zod-schema tools and the human-in-the-loop interrupt.
4. `04-observability.md` — running with LangSmith and reading a real trace.

> Not yet written. The lesson prose follows the WitUS APA-7 citation rule (see `CLAUDE.md`);
> this index is a build note and is exempt.
